@{
    var product = ViewBag.Product as ProductModel;
}

<div class="card bg-warning text-white">
    <h1 class="card-header">Điện thoại @product?.Name</h1>
    <div class="card-body">
        <p class="card-text">
            Giá: @product?.Price
        </p>
    </div>
</div>